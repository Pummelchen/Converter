# Vendored: libbw64

| | |
|---|---|
| Upstream | https://github.com/ebu/libbw64 |
| Version | 0.10.0 (`bw64/version.hpp`, build date 2019-01-21) — the latest upstream release (2019-01-28) |
| License | Apache License 2.0 (`LICENSE` in this directory) |
| Local modifications | none — the headers are vendored verbatim |
| Consumed by | `Sources/BW64Bridge` (in-process BW64 writer used for the `*_BW64.wav` archival deliverable) |

Header-only; only the `bw64/` headers are used. Update by replacing the `bw64/` directory with
the upstream tag and recording the new version and date here.
