# Baseline (§3) — session 2026-09-14 (Swift 6.4 / Xcode 27)

Regression yardstick for the re-audit. Captured on the local host (Apple M2, 8 GB, macOS 27.0
(26A428), Xcode 27.0 (27A266a), Swift 6.4 (`swiftlang-6.4.0.34.1`)) at commit **`40bfadd`**, branch
`audit/2026-09-14`, with `Sources/.build` removed first. Raw reports in `AUDIT/evidence-2026-09-14/`.

No later state may be worse than any row here without a numbered, justified ledger task.

## Build

| Command | Result | Warnings | Errors |
|---|---|---|---|
| `swift build --package-path Sources --build-tests -Xswiftc -warnings-as-errors` (clean, debug) | success, 13.0 s | **0** | 0 |
| `swift build --package-path Sources -c release -Xswiftc -warnings-as-errors -Xcc -Wall -Xcc -Wextra -Xcc -Werror` (strict C++) | success, 17.6 s | **0** | 0 |

Swift language mode 6 (strict concurrency complete) with upcoming features `ExistentialAny`,
`MemberImportVisibility`, `InferIsolatedConformances`, `ImmutableWeakCaptures`: clean under 6.4.

Environment note for the tooling: under Swift 6.4 the SwiftPM build layout changed
(`.build/out/Products/Debug/…`); the index store that the static analyzer consumes only exists when
the build is run with `--enable-index-store`. Recorded in `AUDIT/environment.md`.

## Tests

| Command | Result |
|---|---|
| `swift test --package-path Sources` (serial, canonical) | **263 executed, 0 failures, 0 unexpected, 0 skipped**, 695 s |
| `swift test --package-path Sources --enable-code-coverage` | appended below when the run completes |

Compiler warnings in the suite log: **0**.

## Coverage (`llvm-cov`, production sources + the C++ bridge)

Command (note the Swift 6.4 bundle name `converterTests`, not `converterPackageTests`):

```bash
xcrun llvm-cov report Sources/.build/debug/converterTests.xctest/Contents/MacOS/converterTests \
  -instr-profile Sources/.build/debug/codecov/default.profdata \
  -ignore-filename-regex='Tests|ThirdParty|\.build'
```

| Metric | Baseline 40bfadd (Swift 6.4) | 2026-09-13 baseline (`4bb136a`) |
|---|---|---|
| Lines | **85.42 %** (10 590 counted, 1 544 missed) | 79.57 % |
| Regions | **79.33 %** (4 766 counted, 985 missed) | 72.98 % |
| Functions | **83.71 %** (1 246 counted, 203 missed) | 77.05 % |
| Branches (C++ bridge only) | **53.16 %** (79 counted, 37 missed) | not measured |

Improvement over the previous baseline, so no regression. Full per-file table:
`AUDIT/evidence-2026-09-14/baseline-coverage-report.txt`. Uncovered executable line ranges per
production file: `AUDIT/evidence-2026-09-14/baseline-coverage-gaps.txt`.

Weakest first-party files: `Main.swift` 0 % (the whole `@main` entry, lines 5–58, is never
executed by the suite), `Actions.swift` 74.66 % lines / 65.36 % regions (332 uncovered lines),
`DependencyBootstrap.swift` 67.80 % lines (install paths), `AudioPipeline.swift` 83.19 % lines
(400 uncovered lines, mostly error branches), `ValidationPipeline.swift` 90.48 % lines, the BW64
bridge 69.67 % lines / 53.16 % branches. These are Phase C tasks, not baseline rows.

## Linters / analyzers / type checker

| Tool | Result |
|---|---|
| swiftc, language mode 6, `-warnings-as-errors` | 0 diagnostics |
| swiftlint 0.65.1, default rules, `Sources/converter` + `Sources/Tests` | **471** violations (452 warning-level, **19 error-level**). By rule (error + warning): line_length 398 (0+398), inclusive_language 22 (0+22), function_body_length 21 (4+17), file_length 11 (6+5), cyclomatic_complexity 8 (3+5), type_body_length 6 (6+0), large_tuple 3 (0+3), function_parameter_count 2 (0+2). Detail: `baseline-swiftlint-summary.txt`. The `scripts/lint-budget.json` ratchet records the same 471/19 and the same per-rule counts. |
| periphery 3.8.0 (`--retain-public`) | **23** declarations flagged, **all `assignOnlyProperty`** (0 `unused`): value-record and Hashable-key fields that periphery does not attribute to synthesized `==`/`hash`. Retained with that reason by the 2026-09-13 audit. |
| cppcheck 2.21.0 (`--enable=all`) on `bw64_bridge.cpp` | **0** first-party findings (vendored-header notes only) |
| clang-tidy (LLVM 23.1.1) `bugprone-*,cert-*,clang-analyzer-*,performance-*,misc-*` on `bw64_bridge.cpp` | **2** first-party findings: `misc-const-correctness` at `bw64_bridge.cpp:208` (`chnaChunk`), `bugprone-easily-swappable-parameters` at `bw64_bridge.cpp:272` (3 adjacent numeric parameters) |

## Security / SAST / secrets / dependencies

| Tool | Result |
|---|---|
| semgrep 1.176.0 (`p/swift`, `p/c`, `p/security-audit`, `p/secrets`), `Sources` minus ThirdParty | **0** findings |
| gitleaks 8.30.1, full history (`gitleaks git`) | **1** finding, classified as a **false positive**: rule `generic-api-key` on `Sources/converter/Config.swift` in commit `6cee542`, where the parameter label `codecKey:` sits next to the config-key *name* `"SHORT_MP4_VIDEO_CODEC"`. No credential; the value is never echoed. A narrow scanner allowlist is a Phase B task. |
| trufflehog 3.97.4, full history (second engine) | **0** verified, **0** unverified secrets (11 595 chunks, 112 MB) |
| Dependency / CVE | No SwiftPM dependencies (no `Package.resolved`); vendored libbw64 0.10.0 == latest upstream release (2019-01-28), no advisories on record. Runtime tools ffmpeg 9.0.1 / ImageMagick 7.1.2-31 are Homebrew-managed and outside the build. |

## Repository hygiene at baseline

- Working tree clean at `40bfadd`; `Sources/.build/` and `Output/*` ignored.
- Release binary `converter` (1 717 352 bytes) committed; `docs/converter.sha256` verifies; its
  provenance records Swift 6.3.3 / Xcode 26.6 — **built before the 6.4 standard, so a rebuild is a
  Phase B task**.
- LICENSE (MIT) present; third-party attribution present in `README.md` and `THIRD-PARTY`-style
  sections.
- CI (`.github/workflows/ci.yml`) pins `actions/checkout` by SHA, verifies the binary checksum, runs
  the lint budget, the warnings-as-errors build, the strict C++ build and the suite — but on
  `runs-on: macos-26` with an assertion of **Swift 6.3.3**, i.e. below this session's standard.
- No tags, no releases; `docs/RELEASE_CHECKLIST.md` unchecked.

## Sanitizers

_To be captured in Phase C/E; the 2026-09-13 audit recorded 0 reports for ASan/UBSan/TSan at
4bb136a. Re-run under Xcode 27 is a Phase C task._
